kind: HTTPRoute
apiVersion: gateway.networking.k8s.io/v1alpha2
metadata:
  name: k8s-gateway-api-demo
spec:
  parentRefs:
  - name: gateway
    namespace: istio-system
  hostnames: ["foo.example.com"]
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
      - name: k8s-gateway-api-demo-service-ff3b85e
        port: 8080
        weight: 100
